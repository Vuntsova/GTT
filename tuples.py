# Tuple - can’t be changed once declared
# Use normal brackets not square brackets
# ex_tuple = ( 4,5 )

coordinates_tuple = (4, 6)
coordinates_list = [(4, 6), (8, 9)]
coordinates_list.insert(1, 5)

print(coordinates_tuple)
print(coordinates_list)